"""AGUSTA demonstration environment.

Builds a coherent, interconnected SOC environment so a fresh deployment can be
explored immediately instead of starting empty.

See ``provenance.py`` for exactly which parts are real public reference data and
which parts are synthetic. Nothing in here is real observed telemetry.
"""

DEMO_DATA_VERSION = "1.0.0"
